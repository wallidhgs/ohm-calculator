{
    "dbConfig": {
        "mysqlConfig": {
            "host": "localhost",
            "user": "root",
            "password": "toor",
            "database": "ohm_db"
        },
        "querys": {
            "ohmRange": "SELECT color, band, multiplier, tolerance FROM ohm_values ORDER BY id ASC;"
        }
    }
}